/*某微店要根据库存状态随时更新商品列表。一旦该商品库存为0就要将其下架，并在商品列表上删除该商品。该微店的商品数为n （3=<n<=100），编程实现以下功能：
1）	先将商品数及商品代码输入机器，输入数据格式："%d"
2）	再将目前库存显示为0的一个商品代码输入，输入数据格式："%d"。如果该代码存在，则将其删除，删除成功后将更新后的商品列表输出到屏幕上。更新后的数据将保持输入商品代码顺序输出，无需排序调整，输出数据格式："%d "。 如果在商品代码不存在 则输出"Wrong value!"


输出数据格式："%d "
    当输入商品代码不存在时 则输出"Wrong value!"

建议使用给定的函数原型编写并使用函数。
函数原型：
int Del(int a[], int account, int n);
//数组a保存商品编号，account为商品库存为0 待删除的商品号，n商品个数
//成功删除商品编号，函数返回剩余商品个数；若待删除商品号不存在时，函数返回-1。
//要求：剩余商品编号在数组中必须按原始输入的商品编号顺序连续存储。

运行样例1：
6↙
52108↙
31205↙
31220↙
15492↙
64789↙
54123↙
15492↙
52108 31205 31220 64789 54123

运行样例 2：
7↙
51203↙
52103↙
55102↙
60315↙
12352↙
15462↙
11240↙
11111↙
Wrong value!*/




#include<stdio.h>
int Del(int a[], int account, int n)
{
	int i,t=-1;
	for(i=0;i<=n;i++)
	{
		if(a[i]==account)
		{
			for(;i<n-1;i++)
			{
				a[i]=a[i+1];
			}
			t=0;
			break;
		}
	}
	return t;
} 

int main(void)
{
	int n,i,account,t;
	scanf("%d",&n);
	int a[n];
	for(i=0;i<=n-1;i++)
	{
		scanf("%d",&a[i]);
	}
	scanf("%d",&account);
	t=Del(a,account,n);
	if(t==-1) printf("Wrong value!");
	else
	{
		for(i=0;i<=n-2;i++) printf("%d ",a[i]);
	}
	return 0;
}








